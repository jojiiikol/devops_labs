[tox]
envlist = py39
skipsdist = True

[testenv]
changedir = backend
deps =
    -r/backend/requirements.txt
    pytest
    pytest-cov
commands =
    python -m pytest --cov=app --cov-report=xml --cov-config=../tox.ini --cov-branch